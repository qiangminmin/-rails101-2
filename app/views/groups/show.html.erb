<div class="col-md-12">
  <div class="group">
    <% if current_user && current_user == @group.user %>
    
<% end %>
  </div>
  <h2><%= @group.title %></h2>
  <p><%= @group.description %></p>
</div>
